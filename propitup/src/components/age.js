import React, { Components } from 'react';
import Card from './card';

const Age = (props) => {

        return(
            <div>
                Age: {props.age}
            </div>
        )
}
export default Age;